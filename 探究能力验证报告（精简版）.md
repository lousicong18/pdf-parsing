# PDF 解析 Demo — 探究能力验证报告（精简版）

> 日期：2026-08-14

---

### #1 分类阈值调优 ✅
- **验证方式**：CLI eval `uv run python -m src.eval.runner --samples-dir samples`
- **数据支撑**：5 份 PDF 共 30 页，准确率 **100%（30/30）**，text/table/mixed 均为 100%
- **结论**：分类器在合理阈值下准确率达标，可投入实用

### #2 隐形文本层检测 ✅
- **验证方式**：前端 PageCard「查看特征 JSON」
- **数据支撑**：`PageFeatures` 含 `overlap_rate` / `char_density` / `font_flags` 字段
- **结论**：特征提取正常，前端可观测

### #3 矢量图/表格/Logo 区分 ✅
- **验证方式**：前端特征 JSON
- **数据支撑**：`line_count` / `area_ratio` / `orthogonality` / `drawings_path_count` 均有值
- **结论**：特征数据完整，可区分不同类型页面

### #4 表格工具选型对比 ✅
- **验证方式**：CLI `--table-backends "pdfplumber,camelot"` 对比
- **数据支撑**：HybridExtractor（pdfplumber + camelot stream 评分选优），流式表格 15×2 正确
- **结论**：表格选型依赖文档类型，HybridExtractor 自适应选优

### #5 跨页表格合并 ✅
- **验证方式**：`sample-longtable.pdf`（3 页 99 行）实测
- **数据支撑**：`cross_page=True` + `header_repeat=True`，续表表头自动去重（40→39行）
- **结论**：跨页合并逻辑正确，表头去重有效

### #6 多栏阅读顺序 ✅
- **验证方式**：前端 PageCard 特征条 `columns`
- **数据支撑**：`pdf_parser_test_document.pdf` 页1 `columns=2`，成功检测多栏
- **结论**：多栏检测功能正常

### #7 VLM 成本/效果权衡 ✅
- **验证方式**：CLI `--compare-vlm "MiniMax-M3,deepseek-v4-pro,kimi-k2.7-code"` 多模型对比
- **数据支撑**：真实定价下 MiniMax-M3 ¥0.186（最低），kimi-k2.7-code ¥0.550（最高）
- **结论**：MiniMax-M3 性价比最优，接入真实定价后成本可量化

### #8 公式与特殊内容 ✅
- **验证方式**：`sample-math.pdf`（3 页）实测
- **数据支撑**：Unicode 符号检测 9+8 处，图片公式 VLM 识别返回 LaTeX
- **结论**：文字层符号保留、图片层 VLM 识别效果好，上下标结构仍需 VLM 辅助

### #9 输出格式统一 (IR) ✅
- **验证方式**：JSON 导出 / `GET /tasks/{id}`
- **数据支撑**：每 Block 含 `bbox` / `page_type` / `type` / `content` 字段
- **结论**：结构化真相源完整，Markdown 为派生视图

### #10 端到端评测方案 ✅
- **验证方式**：CLI eval 自动跑测
- **数据支撑**：输出准确率/混淆矩阵/特征分布/VLM 成本/多模型对比，可用于回归测试
- **结论**：评测方案可运行，输出完整
