# PDF 图文混排解析 Demo — 探究能力与多模型使用指南

> 本文档面向研发，说明 Demo 的「可实验的探针平台」能力如何测试、多 VLM 模型如何注册与对比。对应设计文档 `spec.md` §11「研发探究能力」与 `code_plan.md` §11.10「VLM 模型选型对比」。

---

## 1. 探究能力总览（10 项）

| # | 探究问题 | 怎么测 | 入口 |
|---|---------|--------|------|
| 1 | 分类阈值调优 | 改 `.env` `CLS_*`，eval 回归对比 | `src/eval/runner.py --cls-overrides` |
| 2 | 隐形文本层检测 | 观察 `PageFeatures.overlap_rate` / `char_density` / `font_flags` | 前端 PageCard「查看特征 JSON」 |
| 3 | 矢量图/表格/Logo 区分 | 观察 `line_count` / `area_ratio` / `orthogonality` / `drawings_path_count` | 前端 PageCard「查看特征 JSON」 |
| 4 | 表格工具选型对比 | 同样本集跑多 backend 对比 | `src/eval/runner.py --table-backends` |
| 5 | 跨页表格合并策略 | 观察 `TableData.cross_page` 标注与 eval 日志 | 前端 TableBlock「跨页续表」标签 |
| 6 | 多栏阅读顺序 | 观察 `PageFeatures.columns` 与阅读顺序 | 前端 PageCard 特征条 `columns` |
| 7 | VLM 成本/效果权衡 | 多模型 eval 对比成本/耗时/描述质量 | `src/eval/runner.py --compare-vlm` |
| 8 | 公式与特殊内容 | `probe_latex` 探针验证 `get_text("latex")` | eval 探针日志 |
| 9 | 输出格式统一 (IR) | 验证 Block 模型为统一 JSON 结构 | JSON 导出 / `GET /tasks/{id}` |
| 10 | 端到端评测方案 | 标注集 runner 输出准确率 + 混淆矩阵 | `src/eval/runner.py` |

### 1.1 快速体验（无标注集）

若你**暂时没有标注样本集**，可先用「手工 PDF + 目测」跑通流程：

```bash
# 启动后端
uv run python -m src.server        # http://localhost:8082

# 用任一 PDF 上传（curl 示例）
curl -X POST http://localhost:8082/api/v1/parse \
  -F "file=@/path/to/your.pdf" \
  -F "vlm_model=glm-4v"

# 拿到 task_id 后轮询
curl http://localhost:8082/api/v1/tasks/{task_id}

# 导出结构化真相源（JSON，保全精度）
curl -O http://localhost:8082/api/v1/tasks/{task_id}/export?format=json

# 导出派生视图（Markdown，人读）
curl -O http://localhost:8082/api/v1/tasks/{task_id}/export?format=markdown
```

在返回的 `pages[].features` 中即可看到 #2/#3/#6 的特征向量；`pages[].blocks[]` 中即可看到 #9 的 Block IR。

前端启动后访问 http://localhost:5173 可用可视化界面查看上述内容（PageCard 特征条 + 「查看特征 JSON」弹层 + MetricsPanel）。

```bash
cd frontend && npm install && npm run dev
```

---

## 2. 端到端评测方案（#10，核心）

### 2.1 评测 harness 架构

```
samples/
├── report.pdf            # 测试样本
├── report.label.json     # 人工标注真相
├── scan_doc.pdf
├── scan_doc.label.json
└── ...

↓ runner.py 跑完

report.json               # 结构化结果（准确率/混淆矩阵/特征分布/对比/diff）
report.md                 # 可读报告（混淆矩阵表/badcase 列表）
```

### 2.2 准备标注样本集

按 spec §9 验收口径：**每类 ≥ 20 页、人工标注核对**。

**目录约定**：

```
samples/
├── your_sample.pdf
└── your_sample.label.json     # 与 PDF 同名、同目录
```

**label.json 格式**：

```json
{
  "expected_types": ["text", "table", "mixed", "scan"],
  "expected_blocks": [
    { "type": "text" },
    { "type": "table" }
  ]
}
```

- `expected_types`：**必填**，按页序列出每页期望类型（text / table / mixed / scan），用于计算**分类准确率**与**混淆矩阵**。
- `expected_blocks`：**可选**，按页序列出每页期望 Block 类型序列，用于输出 diff / badcase。

> 提示：可用「纯手工 PDF」起步——用 PyMuPDF / ReportLab 生成含已知类型页面的 PDF，标注即真相。后续再替换为真实业务样本。

### 2.3 运行评测

```bash
# 基础评测（分类准确率 + 混淆矩阵 + 特征分布 + VLM 成本）
uv run python -m src.eval.runner \
  --samples-dir samples \
  --report report.json

# 改分类阈值后回归对比
uv run python -m src.eval.runner \
  --samples-dir samples \
  --report report_cls.json \
  --cls-overrides "CLS_LINE_COUNT_TABLE=15,CLS_AREA_RATIO_MIN=0.1"

# 表格 backend 对比（#4）
uv run python -m src.eval.runner \
  --samples-dir samples \
  --report report_table.json \
  --table-backends "pdfplumber,camelot"

# VLM 模型对比（#7，详见第 3 节）
uv run python -m src.eval.runner \
  --samples-dir samples \
  --report report_vlm.json \
  --compare-vlm "glm-4v,qwen-vl-max,deepseek-vl"
```

### 2.4 输出报告内容

`report.json` 结构：

```json
{
  "classification_accuracy": { "overall": 0.92, "per_type": { "text": 0.95, "table": 0.90, "mixed": 0.88, "scan": 0.93 } },
  "confusion_matrix": { "labels": ["text","table","mixed","scan"], "matrix": [[..]] },
  "feature_distribution": { "text": { "line_count": {...}, "area_ratio": {...} } },
  "table_backend_compare": { "pdfplumber": {...}, "camelot": {...} },
  "vlm_model_compare": { "glm-4v": {...}, "qwen-vl-max": {...} },
  "vlm_cost_aggregate": { "total_cost": 0.012, "by_kind": {"image": 5, "scan": 2} },
  "badcases": [{ "page": 3, "expected": "table", "actual": "mixed" }]
}
```

`report.md` 为可读版本，含混淆矩阵表、特征分布表、badcase 列表。

### 2.5 验收门槛（spec §9）

> **分类准确率 ≥ 90%**（各类型分别统计）方视为 Demo 完成；badcase 仍需记录以便迭代。

即 `report.json.classification_accuracy.overall ≥ 0.9` 且 `per_type.*` 均 ≥ 0.9。

---

## 3. 多 VLM 模型注册与使用（#7）

### 3.1 模型注册表（models.json）

系统支持**命名模型注册表**，可跨通义 / 智谱 / DeepSeek / OpenAI 任意组合。

**文件位置**：项目根目录 `models.json`（路径可由 `.env` `VLM_MODELS_FILE` 覆盖）。

**格式**：

```json
{
  "glm-4v": {
    "name": "glm-4v",
    "base_url": "https://open.bigmodel.cn/api/paas/v4",
    "api_key": "your-zhipu-key",
    "model": "glm-4v"
  },
  "qwen-vl-max": {
    "name": "qwen-vl-max",
    "base_url": "https://dashscope.aliyuncs.com/compatible-mode/v1",
    "api_key": "your-qwen-key",
    "model": "qwen-vl-max"
  },
  "deepseek-vl": {
    "name": "deepseek-vl",
    "base_url": "https://api.deepseek.com/v1",
    "api_key": "your-deepseek-key",
    "model": "deepseek-vl"
  },
  "gpt-4o": {
    "name": "gpt-4o",
    "base_url": "https://api.openai.com/v1",
    "api_key": "your-openai-key",
    "model": "gpt-4o"
  }
}
```

> ⚠️ `models.json` 含 API Key，**不要提交到 Git**（建议加入 `.gitignore`）。

### 3.2 无注册表时的回退

若 `models.json` 不存在，系统自动回退到 `.env` 单组配置：

```ini
VLM_BASE_URL=https://open.bigmodel.cn/api/paas/v4
VLM_API_KEY=your-key
VLM_MODEL=glm-4v
VLM_DEFAULT_MODEL=glm-4v
```

此时 `GET /api/v1/models` 返回 `{"models": ["glm-4v"], "default": "glm-4v"}`。

### 3.3 单任务指定模型

**前端**：上传时 `Uploader.vue` 的 `el-select` 会调用 `GET /api/v1/models` 填充下拉，选中模型后随 `vlm_model` 字段提交。

**API 直传**：

```bash
# 指定 qwen-vl-max 解析
curl -X POST http://localhost:8082/api/v1/parse \
  -F "file=@/path/to/doc.pdf" \
  -F "vlm_model=qwen-vl-max"
```

`vlm_model` 必须为 `models.json` 中的注册名；不传则用 `VLM_DEFAULT_MODEL`。

### 3.4 多模型对比评测（eval 模型对比）

```bash
uv run python -m src.eval.runner \
  --samples-dir samples \
  --report report_vlm.json \
  --compare-vlm "glm-4v,qwen-vl-max,deepseek-vl"
```

对**同一标注样本集**，每个模型各跑一遍（mixed / scan 页走该模型），输出**模型对比表**：

| 维度 | 说明 |
|------|------|
| mixed 图片描述与期望相似度 | 多模态理解质量 |
| scan OCR 与期望文本相似度 | OCR 质量 |
| cost_usd | 总成本 |
| latency_ms | 总耗时 |
| tokens | 总 token 消耗 |
| 调用数 | VLM 调用次数 |

报告 `report_vlm.json` 中 `vlm_model_compare` 节与 `report.md` 中「VLM 模型对比」章节呈现上述内容。

### 3.5 关键实现位置

| 关注点 | 文件 |
|--------|------|
| 模型注册表加载 / 回退 | `src/utils/vlm_models.py` |
| VLM 调用（按 name 选 ModelConfig） | `src/parser/vlm.py` `_call_vlm` |
| 单任务 vlm_model 透传 | `src/task_manager/task_service.py` `run_parse(..., vlm_model=)` |
| 模型对比评测 | `src/eval/runner.py` `--compare-vlm` + `src/eval/metrics.py` `vlm_model_compare` |
| 前端模型下拉 | `frontend/src/components/Uploader.vue` + `frontend/src/services/parse.ts` `getModels` |
| 可用模型列表 API | `GET /api/v1/models` → `src/controller/models_controller.py` |

---

## 4. 分类阈值调优（#1）

### 4.1 可配阈值一览（`.env`）

| 变量 | 默认 | 含义 |
|------|------|------|
| `CLS_LINE_COUNT_TABLE` | 20 | table 判定：drawings 线数阈值 |
| `CLS_TEXT_BLOCKS_MIN` | 4 | table 判定：最少文本块数 |
| `CLS_LINE_COUNT_TEXT` | 10 | text 判定：线数上限 |
| `CLS_AREA_RATIO_MIN` | 0.15 | table 判定：线密集区占页面面积下限（Logo 过滤） |
| `CLS_DRAWINGS_PATH_MIXED` | 200 | mixed 判定：drawings 路径数下限（矢量图情形） |

### 4.2 调优流程

1. 改 `.env` 中 `CLS_*` → 重启后端 → 跑 eval → 看 `report.json.classification_accuracy`。
2. 或直接 CLI 覆盖（不改 `.env`）：

```bash
uv run python -m src.eval.runner \
  --samples-dir samples \
  --report report_a.json \
  --cls-overrides "CLS_LINE_COUNT_TABLE=15,CLS_AREA_RATIO_MIN=0.1"
```

3. 对比 `report_a.json` vs `report_b.json` 的混淆矩阵，定位哪类改善 / 恶化。

### 4.3 特征分布辅助分析（#2/#3）

调阈值时配合 `report.json.feature_distribution`：按 PageType 聚合的 `PageFeatures` 特征向量表（`line_count` / `area_ratio` / `overlap_rate` / `char_density` / `orthogonality` / `drawings_path_count` / `columns` 各类型统计），辅助判断阈值是否合理。

---

## 5. 表格工具选型对比（#4）

```bash
uv run python -m src.eval.runner \
  --samples-dir samples \
  --report report_table.json \
  --table-backends "pdfplumber,camelot"
```

> 注意：camelot / marker / mineru 需额外安装对应依赖；未安装时工厂抛明确错误。

对比维度：行列准确率、合并单元格召回、耗时（见 `report_table.json.table_backend_compare`）。

切换默认 backend：改 `.env` `TABLE_EXTRACTOR=pdfplumber|camelot|marker|mineru`。

---

## 6. VLM 成本 / 效果权衡（#7）

### 6.1 单次任务观测

前端 `MetricsPanel` 展示任务级 `TaskMetrics`：`total_cost` / `total_latency_ms` / `by_kind`（image/scan 调用次数）/ `vlm_calls` 明细（per-call：kind / model / latency_ms / tokens / cost_usd / retry / success）。

### 6.2 降本手段

| 手段 | 配置 |
|------|------|
| 降低 DPI | `.env` `VLM_OCR_DPI=150` / `VLM_DESC_DPI=150` |
| 响应缓存 | `.env` `VLM_RESPONSE_CACHE=on`（按 image bytes hash 缓存，避免重复调用） |
| 换低成本模型 | `models.json` 注册低价模型，任务指定或 eval 对比 |

### 6.3 多模型对比

见第 3.4 节。

---

## 7. 公式与特殊内容（#8）

`.env` `FORMULA_MODE=annotate|latex|model`：

- `annotate`（默认）：轻标注，仅标注疑似 LaTeX 碎片。
- `latex`：启用 `probe_latex` 验证 `get_text("latex")` 可用性（记 eval 日志）。
- `model`：预留专用模型接口。

---

## 8. 一键验证清单

研发拿到 Demo 后，按以下顺序跑通，即可验证全部 10 项探究能力：

```bash
# 0. 环境准备
uv sync                                # 后端依赖
cd frontend && npm install             # 前端依赖

# 1. 启动后端
uv run python -m src.server            # 8082

# 2. 单文件跑通（#7 单模型 + #9 IR 输出）
curl -X POST http://localhost:8082/api/v1/parse -F "file=@test.pdf"
curl http://localhost:8082/api/v1/tasks/{task_id}/export?format=json

# 3. 准备样本集（#10）
mkdir -p samples
cp /path/to/known.pdf samples/
# 手工写 samples/known.label.json（见 2.2 格式）

# 4. 跑评测（#1/#2/#3/#4/#6/#10）
uv run python -m src.eval.runner --samples-dir samples --report report.json
cat report.md

# 5. 多模型对比（#7）
# 先写 models.json（见 3.1），再：
uv run python -m src.eval.runner --samples-dir samples --report report_vlm.json \
  --compare-vlm "glm-4v,qwen-vl-max"

# 6. 前端可视化（#2/#3/#5/#6/#7 MetricsPanel）
cd frontend && npm run dev              # 5173，上传后看 PageCard 特征 + MetricsPanel
```

---

## 9. 常见问题

| 问题 | 原因 | 解决 |
|------|------|------|
| `GET /api/v1/models` 只返回 1 个模型 | `models.json` 不存在，回退 `.env` | 按 3.1 创建 `models.json` |
| mixed / scan 页全显示「未识别」 | 无有效 API Key / Key 错误 | 检查 `.env` `VLM_API_KEY` 或 `models.json` 对应 Key |
| 分类准确率上不去 | 阈值不适配当前样本集 | 用 `--cls-overrides` 调阈值 + 看 `feature_distribution` 辅助定位 |
| camelot backend 报依赖缺失 | camelot-py 未安装 | `uv add camelot-py[cv]` 或用默认 pdfplumber |
| eval 报告为空 | `samples/` 下缺少 `.label.json` | 按 2.2 补标注文件 |

---

## 10. 验收口径对齐（spec §9 / §11）

| 验收项 | 达标线 | 验证命令 |
|--------|--------|----------|
| 分类准确率 | 总体 + 各类型 **≥ 90%** | `report.json.classification_accuracy` |
| 分类阈值可配 | `.env` `CLS_*` / `--cls-overrides` 切换回归 | 跑两次 eval 对比 |
| 表格 backend 可切 | `.env` `TABLE_EXTRACTOR` / `--table-backends` | 跑两次 eval 对比 |
| VLM 模型可切换 | `models.json` + `vlm_model` 参数 + `--compare-vlm` | 跑 eval 对比 |
| 评测报告输出 | `report.json` + `report.md`（混淆矩阵 + badcase） | 跑一次 eval 检查输出 |
